# Current to-do list

**Revised 2026-09-09** after the supervised bench session. This is a *status*
document: it gets rewritten, not appended to. Filename keeps its original date
so existing links hold.

Baseline: `main` at `6a1d6a5`, one commit ahead of `origin/main`.

## Closed since the reassessment

- ~~Coordinate with Claude and merge reviewed branch `7552c61`~~ — integrated
  as `8d16c93` (fast-forward, no conflicts), merged and **deployed** during the
  09-08 session. 299 tests pass.
- ~~Identify the physical plate and back up its state~~ — snapshot in
  `C:\Users\sdl2\bench-20260908\`; layout confirmed spectroscopically, including
  orientation (A4 read 1.1435 against 1.1434 three days earlier).
- ~~Deploy and restart under supervision, preserving PySpin~~ — `camera_ready:
  true` after restart; PySpin survived the 09-07 environment rebuild.
- ~~Verify optics in Gen5, then reproduce through the API~~ — **superseded.**
  Resolved directly by opening the turret: no objectives are fitted. Gen5
  cannot do better than the hardware, so the cross-check would add nothing.
  See [`BENCH_2026-09-08.md`](BENCH_2026-09-08.md).
- ~~Stop reporting condenser annuli as fitted objectives~~ — `6a1d6a5`.

## Open — hardware, in the order that spends least on the wrong part

Full reasoning in [`FLUORESCENCE_IMAGING.md`](FLUORESCENCE_IMAGING.md).
Fluorescence **imaging** is a confirmed requirement, so cubes are needed, not
optional.

1. **Measure the emitter's excitation/emission maxima** before buying any
   filter cube. Column 12 of the current plate carries an emissive solid of
   unknown spectrum; Rhodamine 6G in column 9 is the control (should recover
   ex ~526 / em ~555). Coarse 25 nm, then fine 5 nm. Emission must sit 20-30 nm
   redder than excitation, and focal height >= 5.8 mm on the 19 mm plate or the
   read is refused with `5B00`. Needs the plate back and ~30 min.
2. **Physically check which LED cubes are fitted.** The driver never queries
   that turret, so `/status` cannot answer it. A filter cube without its
   matching LED cube is inert. Same class of unknown the objective turret was.
3. **Order the cube pair** chosen from step 1 — filter cube plus matching LED
   cube. Part numbers in `FLUORESCENCE_IMAGING.md`.
4. **4X phase objective `1320515`** — ordered 2026-09-09. Its annulus
   (`1320520`) is already fitted, so phase works on arrival. Note NA 0.13
   collects ~1/8 the light of a 20X at NA 0.45; if step 1 shows weak emission,
   `1320517` (20X phase, annulus also fitted) matters more than a second cube.
5. **Verify the imaging path end to end** when parts arrive: fit-out
   photographed, `installed_filters` non-empty, focused *brightfield* with
   credible scale first, then fluorescence, then µm/px calibration against the
   9 mm well pitch.

## Open — bench, needs the instrument free

6. **Verify the capture sidecar carries a real `git_revision`.** No capture has
   been taken since the deploy. Windows services do not inherit an interactive
   `PATH`; if `git` is unreachable the field is silently `null` and the
   provenance is decoration. One capture settles it.
7. **Turn `ftdi_trace` back off** in `config.toml` and restart. Still `true`,
   logging every byte.
8. **Luminescence H11 / G12 / H12 / full plate**, one case at a time, at 7.0 mm
   focal height. Column 12's emissive solid makes H12 a real target rather than
   an arbitrary corner. Save request, reply, timing and trace per case.
9. **Front-panel eject detection.** Press out, press in, with the trace on.
   Idle traffic is one `h` poll every 4.4 s, so anything unsolicited stands
   out. Currently recorded in `HANDOFF.md` as impossible; never actually tested.
10. **Qualify heating and shaking** on a dedicated sacrificial liquid plate.
    Pause shaking before temperature reads — the incubator does not answer
    while the shaker runs.

## Open — software

11. **`over_range` consumers.** The device half shipped and is verified live
    (`{"A2": null, ..., "over_range": ["A2"]}` off the instrument). Nothing in
    `ac-organic-lab` has been touched: the `plate_reader` SkillDefs still
    describe the old shape, and no workflow or plotting code has been checked
    for whether it drops a `null` from a calibration fit. **This is the half
    that prevents a wrong published number.**
12. **Push `main`** — one commit ahead of origin.
13. Explicit **physical-plate confirmation and drawer-state provenance**, so
    restored metadata cannot silently authorise work on the wrong sample. The
    stale `plate_in_reader: true` after the plate was removed was observed live
    on 09-08, with all three reads still offered.
14. A **durable campaign reservation** that survives orchestrator/claim loss.
15. **Publish validated optical capability separately** from camera connection.
    `camera_ready: true` is true and misleading on its own.
16. Consider a **claim-holding helper for `scripts/`** — a missing heartbeat
    cost a 24-well run on 09-07.

## Ask Agilent

One email, three questions:

- Is a **10X phase annulus** available separately, and its part number? Without
  one, a 10X objective (`1320516`) is brightfield-only; the fitted annuli are
  4X, 20X and 40X.
- Which **LED cubes** are fitted to this unit, and which suits the band step 1
  identifies?
- Were **objectives ever supplied** with this instrument? Annuli for all three
  magnifications are installed, which suggests it was specified with them.

Assembly numbers from the 09-08 photographs: `1850503` (imaging module
housing), `1380500 REV E` (cube turret).

Polarised light does **not** need asking — the datasheet settles it. The
Cytation 5 offers no polarised-light imaging; "fluorescence polarization" on
the spec sheet is a plate-reading detection mode, not microscopy. If
birefringence screening matters for crystallisation, that is a separate
instrument.
